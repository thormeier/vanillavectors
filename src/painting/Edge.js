/**
 * An edge. Takes a vector as start and another as destination
 * @param s Starting vector
 * @param d Destination vetor
 * @constructor
 */
var Edge = function (s, d) {
    this.s = s;
    this.d = d;
};
